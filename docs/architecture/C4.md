# Architecture Map — Runner_Dashboard

This document defines the canonical Mermaid C4 architecture map for `Runner_Dashboard`, providing verified context, container boundaries, capability-to-component traceability, and architecture change history.

## Context View (C4Context)

```mermaid
C4Context
    title System Context diagram for Runner_Dashboard

    Person(operator, "Fleet Operator", "Monitors runner health, controls runner lifecycle, and inspects workflows.")
    System(runner_dashboard, "Runner Dashboard", "Operator console and web UI control surface for D-sorganization self-hosted Actions runners.")
    System_Ext(github_api, "GitHub API", "GitHub Actions workflows, runner registrations, queued jobs, and pull requests.")
    System_Ext(maxwell_daemon, "Maxwell_Daemon", "Autonomous AI control plane consumed by the Maxwell tab over HTTP/WS.")
    System_Ext(repo_mgmt, "Repository_Management", "Central fleet policy, lease coordination, and workflow standards.")
    System_Ext(runner_hosts, "Runner Host Nodes", "Physical/virtual machines (DeskComputer, OGLaptop, ControlTower-Runner) hosting runner pools.")

    Rel(operator, runner_dashboard, "Inspects runner fleet and triggers actions", "HTTPS / Browser")
    Rel(runner_dashboard, github_api, "Polls runners, workflows, jobs, and PR status", "REST / GraphQL")
    Rel(runner_dashboard, maxwell_daemon, "Monitors and directs Maxwell agent runs", "HTTP / WebSocket")
    Rel(runner_dashboard, repo_mgmt, "Reads fleet configuration, leases, and policies", "Local FS / Git")
    Rel(runner_dashboard, runner_hosts, "Monitors system resources, systemd units, and disk volume", "SSH / Tailnet HTTP / Subprocess")
```

## Container View (C4Container)

```mermaid
C4Container
    title Container diagram for Runner_Dashboard

    Person(operator, "Operator / Admin", "Accesses dashboard via browser.")

    Container_Boundary(dashboard_boundary, "Runner Dashboard Service") {
        Container(spa_frontend, "Single Page App", "React 19 / TypeScript / Vite", "Interactive web UI with 18+ tabs for fleet monitoring and controls.")
        Container(api_gateway, "FastAPI Application", "Python 3.11+ / FastAPI", "Authentication perimeter, REST routing, WebSocket broadcasting, and middleware.")
        Container(identity_mgr, "Identity & Auth Perimeter", "Python (backend/identity.py)", "Sessions, OAuth, passkeys/WebAuthn, service tokens, and loopback bypass.")
        Container(runner_controller, "Runner & Autoscaler Controller", "Python (backend/runner_autoscaler.py)", "Dynamic pool autoscaling, busy sampling, systemd unit controls, and disk guards.")
        Container(queue_monitor, "Queue & History Engine", "Python (backend/queue_cleanup.py)", "Stale queued job detection, purge-stale automation, and run timing aggregation.")
        Container(fleet_telemetry, "Fleet Autoconfig & Telemetry", "Python (backend/fleet_autoconfig.py)", "Multi-node topology derivation, health aggregation, and machine registry.")
        Container(agent_dispatcher, "Agent & Remediation Dispatcher", "Python (backend/agent_dispatch_router.py)", "Autonomous agent dispatch (Jules, GAAI, Claude, Codex) and lease tracking.")
    }

    System_Ext(github_cloud, "GitHub Cloud", "Actions runner APIs and repository events.")
    System_Ext(maxwell_svc, "Maxwell Daemon Service", "Fleet AI orchestration service.")
    System_Ext(host_systems, "Host Operating Systems", "Linux/WSL systemd units, cgroups, /tmp and storage volumes.")

    Rel(operator, spa_frontend, "Uses browser interface", "HTTPS")
    Rel(spa_frontend, api_gateway, "Consumes REST endpoints & WebSocket", "JSON / HTTP / WS")
    Rel(api_gateway, identity_mgr, "Validates principals and scopes", "In-Process")
    Rel(api_gateway, runner_controller, "Invokes lifecycle operations", "In-Process")
    Rel(api_gateway, queue_monitor, "Queries queue diagnostics", "In-Process")
    Rel(api_gateway, fleet_telemetry, "Retrieves multi-node status", "In-Process")
    Rel(api_gateway, agent_dispatcher, "Dispatches agent remediation tasks", "In-Process")
    Rel(runner_controller, host_systems, "Executes systemctl & queries cgroups", "OS Subprocess")
    Rel(queue_monitor, github_cloud, "Cancels stale runs & fetches jobs", "REST / gh CLI")
    Rel(fleet_telemetry, host_systems, "Probes disk usage & volume limits", "OS Subprocess")
    Rel(agent_dispatcher, maxwell_svc, "Calls Maxwell endpoints", "HTTP REST")
```

## Feature Map

| Capability | Component | Interface | Evidence |
| --- | --- | --- | --- |
| Identity & Auth Perimeter | `backend.identity` | `require_principal`, `require_scope`, `require_orchestrator_peer` | `tests/api/test_orchestrator_api.py` |
| Runner Autoscaling & Watchdog | `backend.runner_autoscaler` | `poll_cycle()`, `_default_pool_overloaded()`, `_sd_notify_socket()` | `tests/test_runner_autoscaler.py` |
| Queue Cleanup & Stale Purge | `backend.queue_cleanup` | `/api/queue/stale`, `/api/queue/purge-stale` | `tests/test_queue_cleanup.py` |
| Fleet Topology Aggregation | `backend.fleet_autoconfig` | `assert_valid_active_registry()`, `derive_pool_topology()` | `tests/test_fleet_autoconfig.py` |
| Host Volume Disk Guard | `backend.host_volume` | `get_host_volume_metrics()`, `assert_volume_headroom()` | `tests/test_host_volume.py` |
| Maxwell Contract Integration | `backend.routers.maxwell` | `/api/maxwell/*`, `MaxwellClient` | `tests/test_maxwell_contract.py` |
| Agent Remediation Dispatch | `backend.agent_dispatch_router` | `/api/remediation/dispatch`, `verify_agent_lease` | `tests/test_agent_dispatch.py` |
| Staff Hub (AI staff runs) | `backend.staff` | `/api/staff/*`, `StaffRunner.submit()`, `RunStore` | `tests/api/test_staff_runner.py` |
| Architecture Map Contract | `scripts.architecture_map_contract` | `validate_architecture_map()` | `tests/test_architecture_map_contract.py` |

## Architecture Change Log

| Date | PR / Issue | Description |
| --- | --- | --- |
| 2026-09-22 | #1194 | Add Staff Hub: named AI staff roles run as local CLI subprocesses with a node-local run store and /api/staff routes |
| 2026-09-10 | #1613 | Adopt maintainable Mermaid C4 architecture-map contract and automated CI verification |
